on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: zmkfirmware/zmk-build-arm:stable
    name: Build Nice!Nano Test Firmware
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Cache west modules
        uses: actions/cache@v4
        env:
          cache-name: cache-zephyr-modules
        with:
          path: |
            modules/
            tools/
            zephyr/
            bootloader/
            zmk/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/west.yml', '**/build.yaml') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.os }}-build-
            ${{ runner.os }}-
      - name: West Init
        run: west init -l config
      - name: West Update
        run: west update
      - name: West Zephyr export
        run: west zephyr-export
      - name: West Build (Nice!Nano v2)
        run: west build -s zmk/app -b nice_nano_v2 -- -DSHIELD=nicenano_test -DZMK_CONFIG="${GITHUB_WORKSPACE}/config"
      - name: Nice!Nano v2 Kconfig file
        run: cat build/zephyr/.config | grep -v "^#" | grep -v "^$"
      - name: Rename zmk.uf2
        run: cp build/zephyr/zmk.uf2 nicenano_test-nice_nano_v2-zmk.uf2
      - name: Archive (Nice!Nano v2)
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: nicenano_test-nice_nano_v2-zmk.uf2
