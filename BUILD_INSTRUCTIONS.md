# Инструкция по сборке прошивки через GitHub Actions

Поскольку локальная сборка требует сложной настройки окружения, проще всего использовать GitHub Actions для автоматической сборки в облаке.

## 🚀 Быстрый старт:

### 1. Создайте репозиторий на GitHub
1. Перейдите на https://github.com/new
2. Назовите репозиторий, например: `nicenano-test`
3. Сделайте его публичным или приватным
4. Нажмите "Create repository"

### 2. Загрузите файлы в репозиторий

```bash
cd /Users/roman/nicenano2
git init
git add .
git commit -m "Initial commit: Nice!Nano v2 + MCP23017 test"
git branch -M main
git remote add origin https://github.com/ВАШ_ЮЗЕРНЕЙМ/nicenano-test.git
git push -u origin main
```

### 3. Автоматическая сборка
- GitHub Actions автоматически начнёт сборку
- Через 5-10 минут прошивка будет готова
- Скачайте файл `nicenano_test-nice_nano_v2-zmk.uf2` из раздела Actions → Artifacts

### 4. Прошивка платы
1. Подключите Nice!Nano v2 к USB
2. Дважды быстро нажмите RESET
3. Появится диск NICENANO
4. Скопируйте скачанный .uf2 файл на этот диск
5. Готово! ✅

---

## 🛠️ Альтернатива: Локальная сборка (сложнее)

Если хотите собирать локально, нужно:
1. Установить Zephyr SDK
2. Установить Python 3.10+
3. Установить west и зависимости
4. Настроить окружение

Это займёт ~30-60 минут настройки.

**Рекомендую использовать GitHub Actions - это проще и быстрее!**
